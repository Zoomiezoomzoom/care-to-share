<!-- Location Finder Section -->
<section id="find-sites" class="py-16 md:py-20 bg-gray-50">
  <div class="container max-w-7xl mx-auto">
    
    <!-- Header -->
    <div class="text-center mb-8">
      <h2 class="text-3xl md:text-4xl font-bold text-[#2D5016] mb-4">
        Find Share to Care Sites
      </h2>
      <p class="text-lg text-gray-600">
        Search by city or ZIP to find collection sites near you
      </p>
    </div>

    <!-- Search & Filter Bar -->
    <div class="bg-white rounded-xl shadow-md p-6 mb-6">
      <div class="flex flex-col md:flex-row gap-4">
        
        <!-- Search Input -->
        <div class="flex-1 relative">
          <svg class="absolute left-3 top-1/2 transform -translate-y-1/2 w-5 h-5 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"/>
          </svg>
          <input 
            type="text" 
            id="siteSearch"
            placeholder="Search by city, ZIP, or business name"
            class="w-full pl-10 pr-4 py-3 border-2 border-gray-200 rounded-lg focus:border-[#C86D4B] focus:outline-none"
          />
        </div>

        <!-- Type Filter -->
        <div class="flex gap-2">
          <button 
            class="filter-btn active" 
            data-filter="all"
            style="padding-top:0.5rem;padding-bottom:0.5rem;border-width:2px;border-color:#e5e7eb;color:#374151;font-weight:600;border-radius:0.5rem;transition:color 0.2s, border-color 0.2s, background 0.2s; background-color:#C86D4B; color:white; border-color:#C86D4B;"
          >
            All Sites
          </button>
          <button 
            class="filter-btn" 
            data-filter="dropoff"
            style="padding-top:0.5rem;padding-bottom:0.5rem;border-width:2px;border-color:#e5e7eb;color:#374151;font-weight:600;border-radius:0.5rem;transition:color 0.2s, border-color 0.2s, background 0.2s;"
          >
            Drop Off
          </button>
          <button 
            class="filter-btn" 
            data-filter="pickup"
            style="padding-top:0.5rem;padding-bottom:0.5rem;border-width:2px;border-color:#e5e7eb;color:#374151;font-weight:600;border-radius:0.5rem;transition:color 0.2s, border-color 0.2s, background 0.2s;"
          >
            Pick Up
          </button>
          <button 
            class="filter-btn" 
            data-filter="both"
            style="padding-top:0.5rem;padding-bottom:0.5rem;border-width:2px;border-color:#e5e7eb;color:#374151;font-weight:600;border-radius:0.5rem;transition:color 0.2s, border-color 0.2s, background 0.2s;"
          >
            Both
          </button>
        </div>

        <!-- Locate Me Button -->
        <button id="locateMe" class="px-6 py-3 bg-[#2D5016] text-white font-semibold rounded-lg hover:bg-[#1F3810] transition-colors whitespace-nowrap">
          📍 Near Me
        </button>

      </div>

      <!-- Active Filters Display -->
      <div id="activeFilters" class="mt-4 hidden">
        <div class="flex items-center gap-2 text-sm text-gray-600">
          <span>Active filters:</span>
          <span class="px-3 py-1 bg-[#C86D4B]/10 text-[#C86D4B] rounded-full">
            Drop Off Only ✕
          </span>
        </div>
      </div>
    </div>

    <!-- Results Count -->
    <div class="mb-4 flex items-center justify-between">
      <p class="text-gray-600">
        Showing <strong id="resultCount" class="text-[#2D5016]">12 sites</strong>
        <span id="locationContext"></span>
      </p>
      <button id="clearFilters" class="text-sm text-[#C86D4B] hover:underline hidden">
        Clear all filters
      </button>
    </div>

    <!-- Split View: List + Map -->
    <div class="grid lg:grid-cols-2 gap-6">
      
      <!-- Left: Results List -->
      <div id="resultsList" class="space-y-4 max-h-[800px] overflow-y-auto pr-4">
        
        <!-- Site Card -->
        <div class="site-card bg-white rounded-lg p-6 shadow-sm hover:shadow-md transition-shadow border-2 border-transparent hover:border-[#C86D4B] cursor-pointer"
             data-lat="32.2988" 
             data-lng="-90.1848"
             data-type="both">
          
          <div class="flex items-start justify-between mb-3">
            <h3 class="text-xl font-bold text-[#2D5016] hover:text-[#C86D4B]">
              Quinn Healthcare
            </h3>
            <span class="px-3 py-1 bg-[#C86D4B] text-white text-xs font-semibold rounded-full">
              BOTH
            </span>
          </div>

          <div class="space-y-2 mb-4 text-gray-600">
            <p class="flex items-center gap-2">
              <svg class="w-4 h-4 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z"/>
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 11a3 3 0 11-6 0 3 3 0 016 0z"/>
              </svg>
              <span>123 Wellness Ave, Jackson, MS 39201</span>
            </p>
            <p class="flex items-center gap-2">
              <svg class="w-4 h-4 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 5a2 2 0 012-2h3.28a1 1 0 01.948.684l1.498 4.493a1 1 0 01-.502 1.21l-2.257 1.13a11.042 11.042 0 005.516 5.516l1.13-2.257a1 1 0 011.21-.502l4.493 1.498a1 1 0 01.684.949V19a2 2 0 01-2 2h-1C9.716 21 3 14.284 3 6V5z"/>
              </svg>
              <a href="tel:6015550123" class="text-[#C86D4B] hover:underline">
                (601) 555-0123
              </a>
            </p>
            <p class="flex items-center gap-2">
              <svg class="w-4 h-4 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"/>
              </svg>
              <span>Mon–Fri: 9am–5pm</span>
            </p>
            <p class="text-sm text-gray-500 font-medium">
              📍 2.3 miles away
            </p>
          </div>

          <div class="flex gap-3">
            <a 
              href="https://www.google.com/maps/search/?api=1&query=123+Wellness+Ave+Jackson+MS+39201"
              target="_blank"
              class="flex-1 text-center px-4 py-2 bg-[#C86D4B] text-white font-semibold rounded-lg hover:bg-[#B55D3B] transition-colors"
            >
              Get Directions →
            </a>
            <button class="px-4 py-2 border-2 border-gray-200 text-gray-700 font-semibold rounded-lg hover:border-[#C86D4B] hover:text-[#C86D4B] transition-colors">
              Details
            </button>
          </div>

        </div>

        <!-- Repeat site cards... -->

      </div>

      <!-- Right: Map -->
      <div class="lg:sticky lg:top-24 h-[800px]">
        <div id="map" class="w-full h-full rounded-xl shadow-lg"></div>
        
        <!-- Map Legend -->
        <div class="mt-4 flex justify-center gap-6 text-sm text-gray-600">
          <span class="flex items-center gap-2">
            <span class="w-3 h-3 rounded-full bg-red-500"></span> Both
          </span>
          <span class="flex items-center gap-2">
            <span class="w-3 h-3 rounded-full bg-blue-500"></span> Drop Off
          </span>
          <span class="flex items-center gap-2">
            <span class="w-3 h-3 rounded-full bg-green-500"></span> Pick Up
          </span>
        </div>
      </div>

    </div>

    <!-- Empty State (hidden by default) -->
    <div id="emptyState" class="hidden text-center py-16">
      <svg class="w-20 h-20 text-gray-300 mx-auto mb-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9.172 16.172a4 4 0 015.656 0M9 10h.01M15 10h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"/>
      </svg>
      <h3 class="text-xl font-bold text-gray-700 mb-2">
        No sites found
      </h3>
      <p class="text-gray-600 mb-6">
        Try adjusting your search or filters
      </p>
      <button onclick="clearAllFilters()" class="px-6 py-3 bg-[#C86D4B] text-white font-semibold rounded-lg">
        Show All Sites
      </button>
    </div>

    <!-- CTA at Bottom -->
    <div class="mt-12 bg-[#FFF8F0] rounded-xl p-8 text-center border-2 border-[#C86D4B]/20">
      <h3 class="text-2xl font-bold text-[#2D5016] mb-3">
        Don't see your business listed?
      </h3>
      <p class="text-gray-600 mb-6">
        Help us reach 100 sites across Mississippi. Register your business as a collection site today.
      </p>
      <a 
        href="#register" 
        class="inline-flex items-center gap-2 px-8 py-4 bg-[#C86D4B] text-white text-lg font-bold rounded-lg hover:bg-[#B55D3B] transition-all shadow-md hover:shadow-lg"
      >
        Register Your Business →
      </a>
    </div>

  </div>
</section>

<style>
  /* 
    Removed @apply usage that referenced unavailable Tailwind utility 'py-2'.
    Instead, all .filter-btn and .filter-btn.active styles are inlined as style attributes on the elements above.
  */

  /* Custom scrollbar for results list */
  #resultsList::-webkit-scrollbar {
    width: 8px;
  }
  
  #resultsList::-webkit-scrollbar-track {
    background: #f1f1f1;
    border-radius: 10px;
  }
  
  #resultsList::-webkit-scrollbar-thumb {
    background: #C86D4B;
    border-radius: 10px;
  }
</style>

<script>
  // JavaScript for filtering, search, and map interaction
  // (Implement search, filter, geolocation, and Google Maps API integration)
</script>